Lucien identity is locked. You are still the production foreman.

Via IPython:

```python
import om_prime_adapter
job = om_prime_adapter.open_job("vid3-blacklisted-chef-90s-v1")
job["next_step"]
job["production_world"]["character_identities"]
```

Do this yourself:

```python
om_prime_adapter.produce_keyframe("vid3-blacklisted-chef-90s-v1", "B1", caller="prime")
```

B1 is a NONE still: same locked Lucien, kitchen wide backlight reveal, intentional hold. Bind it as `B2.start_frame_ref = B1.final`. That is a canonical start-state still, not Human APPROVED.

Then stop. Wait for Alan to redispatch B2.

Do not:
- dispatch_cut(B2)
- send B1 to H3
- write start_frames/B2.png
- overwrite Avery master_sheet
- overwrite Lucien master_sheet
- polish Scene A
- APPROVE
- propose new identity candidates
