# Research Report Explainer — Proposal Director

Turn the evidence map into three distinct editorial treatments and a truthful production plan. `render_runtime` must be chosen independently of the visual style.

Present both Remotion and HyperFrames with report-specific legibility, chart, typography, timing, and availability tradeoffs. The recorded values are `render_runtime="remotion"` and `render_runtime="hyperframes"`. Bundle the comparison into CONTENT_GATE so it does not create a fourth human touchpoint. Do not approve `render_runtime_selection` before the user's gate decision; keep both options in `decision_log`, including any unavailable runtime and its rejection reason.
