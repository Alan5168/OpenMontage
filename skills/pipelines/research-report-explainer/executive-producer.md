# Research Report Explainer — Executive Producer

OpenMontage owns the job from commission through publish package. Resolve and freeze the scenario/profile first, then follow `pipeline_defs/research-report-explainer.yaml` in order. The only human gates are script (Content), scene_plan, and final_gate; do not create approval requests at other stages.

Checkpoint every stage and preserve input hashes. Research, scene truth, repair scope, cost, license, and retention all remain in the OM job root. Pi is an interface and Prime is an executor; neither may keep a parallel workflow.
