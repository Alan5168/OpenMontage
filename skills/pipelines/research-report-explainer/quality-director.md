# Research Report Explainer — Independent QA and Final Director

Run deterministic L0 checks for schema, hashes, paths, duration, resolution, safe area, black/freeze frames, loudness, true peak, tracks, and license. L1 semantic review must cite real frames/timecodes and cannot override an L0 failure.

At the Final Gate present only the proxy and typed evidence. Record accept, one bounded repair, abandon, or technical-pass/content-kill. `publish_package` prepares metadata and retention only; it never publishes automatically.
