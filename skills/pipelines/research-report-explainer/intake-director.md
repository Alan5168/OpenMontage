# Research Report Explainer — Intake Director

For `commission`, create a schema-valid `brief` that records audience, platform, duration, and scenario id. For `report_intake`, fingerprint every PDF/DOCX/image/notebook export into `report_intake`; retain page/version identity and never copy credentials into the project.

Fail if a source lacks a stable hash or version label. The report itself is project-private; only schemas and fixtures belong in the repository.
