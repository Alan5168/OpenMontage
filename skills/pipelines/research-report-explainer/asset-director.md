# Research Report Explainer — Asset Director

Start only after an approved scene-plan checkpoint. Route textless imagery through `image_selector`, motion through `video_selector` or H3 equipment, and charts/readable text through deterministic composition.

Each asset joins by scene id and records prompt, negative prompt, provider, model, seed, size, request id, plan/cost usage, output hash, and license policy. Repair or regenerate only the failed cut.
