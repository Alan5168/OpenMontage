# Comic Nonfiction — Proposal Director

Convert the evidence architecture into three genuinely different content directions and assess their production shape. `render_runtime` is an explicit technical decision, not a style default.

Present both Remotion and HyperFrames with profile-specific tradeoffs, current availability, cost, and reasons. The recorded values are `render_runtime="remotion"` and `render_runtime="hyperframes"`. Bundle the comparison into CONTENT_GATE so it does not add a fourth human touchpoint. Leave `render_runtime_selection` pending until the user chooses at that gate, and retain both options in `decision_log` even when one is unavailable.
