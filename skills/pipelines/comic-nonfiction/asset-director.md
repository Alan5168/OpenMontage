# Comic Nonfiction — Asset Director

After scene-plan approval, route final assets cut by cut. H3/ComfyUI and remote video adapters are equipment behind `video_selector`; API stills stay behind `image_selector`; deterministic charts/text stay in Remotion/FFmpeg.

Record full provenance and continuity inputs. Same-root failure three times stops the route and writes a minimum reproducer; do not batch-draw replacements to chase taste.
