# Anime Hybrid — Seven-Column Scene Director

Create one row per cut using `scene_plan.scenes[].id`. Include layout reference/prompt, acting and camera direction, dialogue, duration, sound intent, world-state/reuse flags, and a bounded motion route.

API layout references guide composition only. Reuse stable layouts and identities; H3 is forbidden until the Sceneplan Gate is approved.
