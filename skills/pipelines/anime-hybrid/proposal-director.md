# Anime Hybrid — Proposal Director

Assess both motion grammar and composition runtime before scripting. `render_runtime` is a separate decision from per-cut H3, 2.5D, or static motion routing.

Present both Remotion and HyperFrames with story-specific benefits, constraints, and availability. The recorded values are `render_runtime="remotion"` and `render_runtime="hyperframes"`. Put the comparison into the existing CONTENT_GATE pack so this assessment does not create a fourth human touchpoint. Do not mark `render_runtime_selection` approved until the user selects it at that gate; record both options in `decision_log` even if one is unavailable.
